import React from 'react';
import PropTypes from 'prop-types';

const Css = (props) => {
  return(
  	<div>Css</div>
  );
};

Css.propTypes = {
  props: PropTypes.object.isRequired
};

export default Css;