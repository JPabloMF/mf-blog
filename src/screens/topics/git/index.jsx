import React from 'react';
import PropTypes from 'prop-types';

const Git = (props) => {
  return(
  	<div>Git</div>
  );
};

Git.propTypes = {
  props: PropTypes.object.isRequired
};

export default Git;