import React from 'react';
import PropTypes from 'prop-types';

const Html = (props) => {
  return(
  	<div>Html</div>
  );
};

Html.propTypes = {
  props: PropTypes.object.isRequired
};

export default Html;