import React from 'react';
import PropTypes from 'prop-types';

const Sass = (props) => {
  return(
  	<div>Sass</div>
  );
};

Sass.propTypes = {
  props: PropTypes.object.isRequired
};

export default Sass;